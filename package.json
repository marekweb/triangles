{
  "scripts": {
    "start": "parcel index.html",
    "serve": "serve",
    "format": "prettier --write --single-quote 'src/**/*.ts'",
    "bundle": "rollup build/run.js --file bundle.js --format esm",
    "build": "tsc && npm run bundle",
    "lint": "tslint -p tsconfig.json"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "prettier": "^1.13.7",
    "rollup": "^0.63.4",
    "serve": "^9.3.0",
    "tslint": "^5.11.0",
    "typescript": "^3.1.0-dev.20180724",
    "typestrict": "0.0.9"
  }
}
